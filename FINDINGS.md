# Example — findings (living quirks log)

Record every source-dialect surprise as you discover it: field shapes, sign
conventions, rounding, settlement timing, pagination, auth quirks, rate limits — and
what does NOT work. This file is the connector's memory.

(empty — start logging as you build)
